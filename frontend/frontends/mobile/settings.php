<?php if (!defined(JZ_SECURE_ACCESS)) die ('Security breach detected.');
$force_resample = $force_resample; // just a reminder that these can be set per-frontend.
$default_resample = "48";
$chart_size = "5";
$chars_per_line = "35";


$main_truncate_length = $chars_per_line - 10;

?>